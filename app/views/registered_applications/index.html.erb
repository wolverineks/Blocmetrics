<h1>Applications</h1>


<div class="row">
  <div class="col-md-8">
  <%= render @registered_applications %>
<%= will_paginate @registered_applications %>
  </div>
  <div class="col-md-4">
    <%# if policy(registered_application.new).create? %>
      <%= link_to "New application", new_user_registered_application_path, class: 'btn btn-success' %>
    <%# end %>
  </div>


</div>
